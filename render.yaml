services:
  - type: web
    name: mindful_minutes
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    region: oregon
    branch: main
    autoDeploy: true
